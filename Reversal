#include <stdio.h>
void main(){
    int n, rev, rem;
    printf("Enter the number to be reversed");
    scanf("%d", &n);
    while (n!=0){
        rem= n%10;
        printf("%d",rem);
        n/=10;
    }
    
}
