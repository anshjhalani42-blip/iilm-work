#include <stdio.h>
void main(){
    int n, num1=0, num2=1,next;
    printf("Enter the number of terms");
    scanf("%d", &n);
    printf("Fibonacci series:");
    for(int i=0; i<=n; i++){
        printf("%d",num1);
        next=num1+num2;
        num1=num2;
        num2=next;
    }
    
}
